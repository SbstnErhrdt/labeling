mutation confirmResetPassword($token: String!, $newPassword: String!, $newPasswordConfirmation: String!) {
    confirmResetPassword(token: $token, newPassword: $newPassword, newPasswordConfirmation: $newPasswordConfirmation)
}
